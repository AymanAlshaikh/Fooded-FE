import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  root: {
    maxWidth: "auto",
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "space-around",
  },
  media: {
    height: 1000,
  },
});
