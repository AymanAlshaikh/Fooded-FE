import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  footer: {
    marginTop: 5,
    paddingTop: 10,
    paddingBottom: 8,
    backgroundColor: "silver",
    // position: "static",
    bottom: 0,
    width: "100%",
  },
});
