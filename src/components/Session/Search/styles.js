import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  center: {
    justifyContent: "center",
    alighItem: "center",
    alignContent: "center",
    textAlign: "center",
  },
});
